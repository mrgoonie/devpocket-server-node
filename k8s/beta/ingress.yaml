apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devpocket-nodejs
  namespace: devpocket-beta
  labels:
    app.kubernetes.io/name: devpocket-nodejs
    app.kubernetes.io/component: api-server
    app.kubernetes.io/part-of: devpocket-platform
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/environment: beta
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/server-snippet: |
      location ~ /.git { deny all; return 403; }
spec:
  ingressClassName: nginx
  rules:
  - host: api.beta.devpocket.app
    http:
      paths:
      - backend:
          service:
            name: devpocket-nodejs
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - api.beta.devpocket.app
    secretName: tls-api-beta-devpocket-app